# Terraform version and provider requirements for monitoring-baseline
# Generated from registry module metadata
# Provider versions reflect requirements from the module

terraform {
  required_version = ">= 1.0.0"

  required_providers {
    aws = {
      source  = "hashicorp/aws"
      version = "~> 5.0"
    }
  }
}
